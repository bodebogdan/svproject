<p id="notice"><%= notice %></p>

<!--<h1>Listing Products</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Width</th>
      <th>Height</th>
      <th>Image</th>
      <th>Price</th>
      <th>Description</th>
      <th>Product type</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.width %></td>
        <td><%= product.height %></td>
        <td><%= image_tag product.image.url(:thumb)%></td>
        <td><%= product.price %></td>
        <td><%= product.description %></td>
        <td><%= product.product_type %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>-->

<%= link_to 'New Product', new_product_path %>

<h4 class="column">Featured products</h4>
<div class="column">
  <ul class="products-grid no-bullet row">
    <% @products.each do |product| %>
      <li class="large-3 medium-4 small-12 column">
        <div class="product-card padly">
          <div class="product-info ell glassy-bg padmy padlx">
            <div class="title">
              <h6><%= link_to (image_tag product.image.url(:thumb)), product %> <span>(<%= product.name %>)</span></h6>
            </div>
            <p class="left price label product-label radius">$ <%= product.price %></p>
          </div>
        </div>
      </li>
    <% end %>
  </ul>
</div>